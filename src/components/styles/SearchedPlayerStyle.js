import styled from 'styled-components';
import { FlexDiv } from './SharedStyles';

export const MarginFlexDiv = styled(FlexDiv)`
  margin: 5px 0;
`
export const SearchedPlayerName = styled.span.attrs()`
  margin-left: 15px;
`
