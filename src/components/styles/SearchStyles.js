import styled from 'styled-components';
import { FlexColumnDiv } from './SharedStyles';

export const SearchedPlayerList = styled(FlexColumnDiv)`
  position: fixed;
  background: lightgray;
  top: 33px;
  width: 600px;
  z-index: 1;
`
